import { BeaconErrorType } from '..';
export declare abstract class BeaconError implements Error {
    name: string;
    message: string;
    title: string;
    description: string;
    constructor(errorType: BeaconErrorType, message: string);
    static getError(errorType: BeaconErrorType): BeaconError;
}
