import { Storage, P2PPairInfo } from '..';
export declare class PeerManager {
    private readonly storageManager;
    constructor(storage: Storage);
    hasPeer(publicKey: string): Promise<boolean>;
    getPeers(): Promise<P2PPairInfo[]>;
    getPeer(publicKey: string): Promise<P2PPairInfo | undefined>;
    addPeer(peerInfo: P2PPairInfo): Promise<void>;
    removePeer(publicKey: string): Promise<void>;
    removePeers(publicKeys: string[]): Promise<void>;
    removeAllPeers(): Promise<void>;
}
