import { Transport } from '..';
export declare class LocalTransport extends Transport {
    static isAvailable(): Promise<boolean>;
    transformer: (message: string) => Promise<string>;
    send(message: string): Promise<void>;
}
