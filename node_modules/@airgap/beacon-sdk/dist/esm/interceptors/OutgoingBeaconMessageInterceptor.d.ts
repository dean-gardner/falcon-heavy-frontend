import { BeaconMessage, BeaconResponseInputMessage } from '..';
import { PermissionManager } from '../managers/PermissionManager';
import { AppMetadataManager } from '../managers/AppMetadataManager';
interface OutgoingBeaconMessageInterceptorOptions {
    beaconId: string;
    request: BeaconMessage;
    message: BeaconResponseInputMessage;
    permissionManager: PermissionManager;
    appMetadataManager: AppMetadataManager;
    interceptorCallback(message: BeaconMessage): void;
}
export declare class OutgoingBeaconMessageInterceptor {
    static intercept(config: OutgoingBeaconMessageInterceptorOptions): Promise<void>;
}
export {};
